{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "hubake build",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                "hubake build app\\demo-hazard3\\app.kdl; if (-not $?) { exit 1 }; ",
                "Expand-Archive -Path 'hazard3-build.zip' -DestinationPath '.' -Force; if (-not $?) { exit 1 }; ",
                "Copy-Item -Path img\\final.elf -Destination final.elf; if (-not $?) { exit 1 }"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}